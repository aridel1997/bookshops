<mvc:View controllerName="ns.bookshop.controller.Worklist"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:semantic="sap.f.semantic"
    xmlns:f="sap.ui.layout.form"
    xmlns:layout="sap.ui.layout"
    xmlns:core="sap.ui.core">

    <semantic:SemanticPage id="page" headerPinnable="false" toggleHeaderOnTitleClick="false" showFooter="true">

        <semantic:titleHeading>
            <Title text="{i18n>worklistTitle}" />
        </semantic:titleHeading>

        <semantic:headerContent>
            <layout:VerticalLayout class="sapUiMediumMarginEnd">
                <ObjectAttribute title="Email" text="info@shopbookshop.com "/>
                <ObjectAttribute title="Address" text="4268 Broadway Oakland, CA 94611"/>
                <ObjectAttribute title="Phone" text="510-907-9649"/>
            </layout:VerticalLayout>
        </semantic:headerContent>

        <semantic:content>
            <Table id="table" width="auto" items="{
                    path: '/Books',
                    sorter: {
                        path: 'ID',
                        descending: false
                    }
                }" noDataText="{worklistView>/tableNoDataText}" growing="true" growingScrollToLoad="true" updateFinished=".onUpdateFinished">

                <headerToolbar>
                    <Toolbar>
                        <Title id="tableHeader" text="Books"/>
                        <ToolbarSpacer />
                        <SearchField id="searchField" tooltip="{i18n>worklistSearchTooltip}" search=".onSearch" width="auto">
                        </SearchField>
                    </Toolbar>
                </headerToolbar>

                <columns>
                    <Column id="nameColumn">
                        <Text text="{i18n>tableNameColumnID}" id="nameColumnTitle"/>
                    </Column>
                    <Column>
                        <Text text="{i18n>tableNameColumnTitle}" />
                    </Column>
                    <Column >
                        <Text text="{i18n>tableNameColumnGenre}" />
                    </Column>
                    <!-- <Column id="unitNumberColumn" hAlign="End"/> -->
                </columns>

                <items>
                    <ColumnListItem type="Navigation" press=".onPress">
                        <cells>
                            <Text maxLines="1" text="{ID}" />
                            <Text maxLines="1" text="{title}" />
                            <Text maxLines="1" text="{genre}" />
                            <Text text="" />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </semantic:content>

        <semantic:footerCustomActions>
            <Button type="Accept" text="Create" press=".onCreateBook"/>
        </semantic:footerCustomActions>

    </semantic:SemanticPage>

</mvc:View>